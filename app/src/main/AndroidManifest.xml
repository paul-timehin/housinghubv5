<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.housinghubv6">

    <uses-permission android:name="android.permission.INTERNET" />


    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".Login.LoginActivity"></activity>
        <activity android:name=".Settings.SettingsActivity" />
        <activity android:name=".Group.GroupActivity" />
        <activity android:name=".YourAccount.YourAccountActivity" />
        <activity android:name=".Favourites.FavouritesActivity" />
        <activity android:name=".Login.AccountChoiceActivity"/>
        <activity android:name=".Login.StudentSignupActivity"/>
        <activity android:name=".Login.LandlordSignupActivity"/>

        <activity
            android:name=".Home.HomeActivity"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>